# Text-to-SQL Assignment

## Database
PostgreSQL with the ClassicModels schema.

## Task 1
SQL Benchmark Dataset: 50 ground-truth queries + evaluation strategy.

## Setup
1. Install PostgreSQL
2. Create a database: `createdb classicmodels`
3. Seed: `psql classicmodels < database/seed.sql`